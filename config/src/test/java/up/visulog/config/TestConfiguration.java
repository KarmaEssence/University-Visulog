package up.visulog.config;

/**
 * @version 1.0
 */
public class TestConfiguration {
    /*
    Do not forget writing tests as soon as class Configuration contains more than just getters
    (or if they or the constructor start doing something smart!).
     */
     /*
     n'oubliez pas d'ecrire des tests après que la class configuration  contienne plus que des getter
     (ou si ils ou les constructeur commencent à faire quelque chose d'intelligent)
     */
}
